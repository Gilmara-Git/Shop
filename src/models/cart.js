class CartItem {
    constructor(quantity, productPrice, productTitle, sum,  productURL){
        this.quantity = quantity;
        this.productPrice = productPrice;
        this.productTitle = productTitle;
        this.sum = sum;
        this. productURL= productURL;
    }
}

export default CartItem;